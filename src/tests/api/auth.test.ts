beforeAll((done) => {
    done();
});

afterAll((done) => {
    done();
});

describe("Users route tests", () => {
    test("Test Get User", () => {
        const num = 10;
        expect(num).toBe(10);
    })

});